export interface Timezone {
    code:       string;     //ISO3166 country codes
    region:     string;
    timezone:   string;
    utc:        string;
    offset:     number;
    description:    string;
    now:        string;
}